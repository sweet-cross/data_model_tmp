# Standard Dimensions

Core dimensions used throughout CROSS. They follow a hierarchical structure, with top-level entries that can be subdivided into more specific levels (for example, a fuel category can be broken down into individual fuels). Standard dimensions use a fixed schema consisting of
   `id`, `level`, `parent_id`, `label`, `description`.

{{ render_dimension_index() }}
